import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'beaver-clock-alarm2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
